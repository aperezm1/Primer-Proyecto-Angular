- En build & output settings:
	1. Build Command: npm run build
	2. Install Command: npm install
	3. Output Directory: dist/primer-proyecto/browser

- Crear un vercel.json con:
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}