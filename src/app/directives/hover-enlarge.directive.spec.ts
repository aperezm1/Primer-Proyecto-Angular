import { HoverEnlargeDirective } from './hover-enlarge.directive';

describe('HoverEnlargeDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverEnlargeDirective();
    expect(directive).toBeTruthy();
  });
});
