import { MostrarElementoSiRolDirective } from './mostrar-elemento-si-rol.directive';

describe('MostrarElementoSiRolDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarElementoSiRolDirective();
    expect(directive).toBeTruthy();
  });
});
