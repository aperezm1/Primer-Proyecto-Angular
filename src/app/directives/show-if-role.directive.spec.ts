import { ShowIfRoleDirective } from './show-if-role.directive';

describe('ShowIfRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfRoleDirective();
    expect(directive).toBeTruthy();
  });
});
