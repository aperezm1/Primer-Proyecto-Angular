import { AgrandarTextoHoverDirective } from './agrandar-texto-hover.directive';

describe('AgrandarTextoHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new AgrandarTextoHoverDirective();
    expect(directive).toBeTruthy();
  });
});
