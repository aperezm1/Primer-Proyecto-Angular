import { EnfocarInputDirective } from './enfocar-input.directive';

describe('EnfocarInputDirective', () => {
  it('should create an instance', () => {
    const directive = new EnfocarInputDirective();
    expect(directive).toBeTruthy();
  });
});
